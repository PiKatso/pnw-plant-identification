$(document).ready(function() {
  // btn leaves-search
  $('#leaves-btn').on("click", function(e){
    $('#cones-search').show().html('<%= j render("cones_form") %>');
    // $('#cones-search').show().after('<%= j render("cones_form") %>');

    $('#leaves-search').hide();
    // e.preventDefault();
  });
  // btn cones-search
  $('#cones-btn').on("click", function(e){
    $('#bark-search').show().html('<%= j render("bark_form") %>');
    $('#cones-search').hide();
    // e.preventDefault();
  });
  // btn bark-search
  $('#bark-btn').on("click", function(e){
    $('#branches-search').show().html('<%= j render("branches_form") %>');
    $('#bark-search').hide();
    // e.preventDefault();
  });
  // btn branches/submit-search
  // none
});
