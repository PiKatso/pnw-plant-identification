$(document).ready(function() {
  $('#cones-search').html('<%= j render("cones_form") %>');
  $('#leaves-search').hide();
});

// // btn cones-search
function show_bark_search(){
  // $(document).ready(function() {
    $('#bark-search').html('<%= j render("bark_form") %>');
    $('#cones-search').hide();
  // });
}
  // btn bark-search
  function show_branches_search() {
    $('#branches-search').html('<%= j render("branches_form") %>');
    $('#bark-search').hide();
  }
